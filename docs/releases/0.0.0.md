# MKV Magic 0.0.0 development package

This is a disposable package-gate fixture, not a public release. It exists to
exercise app assembly, signatures, entitlements, DMG layout, checksums, SBOM,
and signed Sparkle-feed generation without production credentials.
